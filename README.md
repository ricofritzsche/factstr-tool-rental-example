# FACTSTR Tool Rental Example

This repository contains reference implementations of a small tool rental system built with FACTSTR.

The goal is to show the same domain in multiple languages while keeping the important design ideas visible:

- append-only facts
- command context consistency
- functional core / imperative shell
- self-contained features
- feature-owned query models

Implementation details have intentionally not been added yet.
